<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <!-- Tema del modal de captura rápida. Translúcido de arriba abajo para que
         la app que estuviera en pantalla se siga viendo detrás: es lo que hace
         que el atajo se sienta como una ventana flotante y no como "se abrió
         SistemDaily". No lleva windowBackground opaco ni splash, porque
         cualquiera de los dos produciría un parpadeo del color de la app antes
         del primer frame de Flutter.

         backgroundDimEnabled va en false a propósito: el oscurecido lo pinta el
         propio modal en Flutter, y sumar los dos deja la pantalla casi negra.

         No hace falta versión en values-night: el modal resuelve claro/oscuro
         desde la apariencia guardada del usuario, no desde el tema del sistema. -->
    <style name="QuickCaptureTheme" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
