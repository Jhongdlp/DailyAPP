<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="Kapt"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <!-- showWhenLocked/turnScreenOn son necesarios para que el
             full-screen intent de la alarma se pinte por encima del bloqueo
             en un arranque en frío (antes de que Dart pueda pedirlo). Una vez
             iniciada la app sin alarma sonando, main.dart los desactiva vía
             `LockTaskService.showOverLockscreen(false)` para que la app no
             quede accesible desde la pantalla de bloqueo. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Atajos al mantener pulsado el icono. Van colgados de la
                 activity LAUNCHER porque es de donde el launcher los lee. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
            <!-- "Abrir con" y "Compartir con" para libros. Los tipos van
                 acotados a EPUB y PDF a propósito: con `*/*` la app aparecería
                 como destino de cualquier archivo del sistema.
                 El launchMode sigue siendo singleTop (no singleTask, como
                 sugiere receive_sharing_intent) porque la alarma depende de
                 él; los intents entrantes llegan igual por onNewIntent. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/epub+zip"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/epub+zip"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/epub+zip"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>
        </activity>
        <!-- ─── Captura rápida ───
             Modal para anotar un gasto, una tarea o una nota sin entrar en la
             app. Se lanza desde tres sitios: el tile del desplegable, el widget
             de escritorio y los atajos del icono.

             taskAffinity propia (MainActivity usa "") para que el modal NO se
             meta en la pila de la app: debe aparecer sobre lo que el usuario
             tuviera delante y, al cerrarse, devolverlo justo ahí.

             excludeFromRecents porque es una acción de dos segundos, no una
             pantalla a la que volver desde el multitarea.

             exported=true es necesario: quien lanza los atajos es el launcher,
             que es otro proceso. Lo único que puede provocar un tercero es
             abrir un campo de texto vacío, sin acceso a datos. -->
        <activity
            android:name=".QuickCaptureActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity="com.sistemdaily.sistem_daily.quickcapture"
            android:excludeFromRecents="true"
            android:theme="@style/QuickCaptureTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize|stateVisible"/>

        <!-- Tile de ajustes rápidos. El permiso BIND_QUICK_SETTINGS_TILE lo
             exige el sistema para poder enlazar el servicio. -->
        <service
            android:name=".QuickCaptureTileService"
            android:exported="true"
            android:icon="@drawable/ic_quick_capture"
            android:label="@string/quick_capture_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
            <!-- Deja el tile activo mientras el panel está abierto sin tener
                 que desbloquear el móvil; el desbloqueo se pide al pulsarlo. -->
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="false"/>
        </service>

        <!-- Widget de escritorio con los cuatro botones de captura. -->
        <receiver
            android:name=".QuickCaptureWidgetProvider"
            android:exported="false"
            android:label="@string/quick_capture_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_capture_info"/>
        </receiver>

        <!-- Requerido por el paquete `alarm` para la notificación de aviso
             cuando la app es cerrada mientras hay una alarma programada -->
        <service android:name="com.gdelataillade.alarm.services.NotificationOnKillService" />
        <!-- flutter_local_notifications receivers -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
